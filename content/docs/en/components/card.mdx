---
title: Card
description: The `Card` component is a versatile container used to display content in a structured and visually appealing manner.
locale: en
---

## Usage

To use the `Card` component, import it and include it in your JSX as shown below:

```jsx
import Card from "@/components/ui/Card";

const MyComponent = () => {
  return (
    <Card className="shadow-lg p-4 rounded-lg bg-white">
      <h2 className="text-xl font-semibold">Card Title</h2>
      <p className="text-gray-600">This is a description inside the card.</p>
      <Button className="mt-4">Learn More</Button>
    </Card>
  );
};

export default MyComponent;
```
